/**
 * @title 더하기
 * @description 두 수를 더하여 반환합니다.
 * @param a 더할 첫 번째 수
 * @param b 더할 두 번째 수
 * @returns 더한 결과
 * 
 * @example
 * const result = add(1, 2);
 * console.log(result); // 3
 */
export function add(a: number, b: number) {
  return a + b;
}
